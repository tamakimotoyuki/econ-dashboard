# econ-dashboard — 公開可否の札

**公開可否: Public可（生成済みダッシュボードHTMLのみ・noindex前提）**

## このリポに置いてよいもの（これ以外は置かない）

- `sync_to_public.py`（非公開の管理リポ側）が生成した `dashboard*.html` / `index.html` / `robots.txt`
- この札（CLAUDE.md）と README

## 絶対に置いてはいけないもの

- スクリプト・データCSV・運用情報・ルーティン設定（すべて非公開の管理リポ側で管理＝ここには名前も書かない）
- APIキー・トークン・認証情報
- 氏名（ひらがな「たまき」表記を含め、個人を特定しうる情報を生成HTMLに混ぜない）・資産額・年収

## 運用ルール

- **このリポを直接編集しない**＝HTMLは全て管理リポ側で生成し、同期スクリプトでコピーされる（手で直しても次回同期で消える）
- commit は同期スクリプトまたは定期ルーティンが行う。main へ直接 push（PR・ブランチは作らない）
- 検索エンジン非公開（noindex/nofollow + robots.txt）を維持する

最終更新: 2026-09-03
