# AGENTS.md

このファイルは、Codex / ChatGPT など `CLAUDE.md` を自動では読まないエージェント向けの入口です。

## 作業前に必ず読む

作業を始める前に、次の `CLAUDE.md` を必ず読み、そのルールに従ってください。

1. このリポジトリ直下の `CLAUDE.md`
2. 作業対象までの階層にある各 `CLAUDE.md`
3. 田蒔さんのローカルPC上で実行している場合のみ、`C:\Users\tamak\.claude\CLAUDE.md`

クラウド環境からローカルPCのファイルは読めません。その場合は、リポジトリ内で読める `CLAUDE.md` だけを読みます。存在しないファイルを読んだと主張しないでください。

## Git運用

- ユーザーからこのリポジトリへの変更を明示的に依頼された場合、Codex / ChatGPT は編集してよい。
- 変更は作業ブランチにコミットしてpushし、Pull Request（PR）を作成する。
- mainへの直接push、force push、保護ブランチの削除は禁止する。
- PR作成後は、ブランチ保護、CODEOWNERS、承認ルールに従う。
- 既存の未コミット変更を自分のコミットに混ぜない。必要なら隔離したworktreeを使う。

`CLAUDE.md` とGit運用の記述が矛盾する場合は、このファイルの「作業ブランチへpush可・PR必須・main直push禁止」を優先します。それ以外の安全、機密、退避、リポジトリ固有ルールは `CLAUDE.md` に従います。

最終更新: 2026-09-14